apiVersion: v1
kind: Service
metadata:
  name: cdo-be-app
  namespace: cdo-be
  labels:
    app: cdo-be-app
spec:
  # Service chọn các pod có label app=cdo-be-app.
  selector:
    app: cdo-be-app
  ports:
    - name: http
      # Port 80 giúp port-forward và ServiceMonitor dễ đọc hơn.
      port: 80
      targetPort: http
