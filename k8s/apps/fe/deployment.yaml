apiVersion: apps/v1
kind: Deployment
metadata:
  name: cdo-fe-app
  namespace: cdo-fe
  labels:
    app: cdo-fe-app
spec:
  # Một replica là đủ cho FE tĩnh trong lab.
  replicas: 1
  selector:
    matchLabels:
      app: cdo-fe-app
  template:
    metadata:
      labels:
        app: cdo-fe-app
    spec:
      containers:
        - name: nginx
          # Kustomize thay tag image nginx tại kustomization.yaml.
          image: nginx:1.27.3-alpine
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: nginx-content
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: nginx-content
          configMap:
            name: cdo-fe-content
