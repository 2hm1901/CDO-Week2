apiVersion: v1
kind: ConfigMap
metadata:
  name: cdo-nginx-content
  namespace: cdo-nginx
data:
  # Nội dung HTML nhỏ để dễ phân biệt app nginx với backend demo.
  index.html: |
    <!doctype html>
    <html>
      <head>
        <title>CDO Nginx App</title>
      </head>
      <body>
        <h1>CDO Week 2 - Nginx App</h1>
        <p>This is the second ArgoCD child application.</p>
      </body>
    </html>
